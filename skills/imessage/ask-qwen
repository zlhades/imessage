#!/bin/bash
# 获取最后一条消息并让 Qwen 处理

MESSAGE=$(python3 /Users/benson/Documents/incident/skills/imessage/imessage_skill.py last "+18336801616" 2>/dev/null)

if [ $? -eq 0 ]; then
    TEXT=$(echo "$MESSAGE" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('text',''))")
    DATE=$(echo "$MESSAGE" | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('date',''))")
    
    # 直接调用 Qwen Code 处理这条消息
    echo "📱 消息内容："
    echo "$TEXT"
    echo ""
    echo "🤖 正在让 Qwen 分析..."
    echo ""
    
    # 使用 qwen-code CLI 处理
    qwen "分析这条 iMessage 消息：
    
时间：$DATE
内容：$TEXT

请告诉我：
1. 这是什么类型的消息（通知/对话/验证码等）
2. 是否需要回复
3. 是否有重要信息需要提取"
else
    echo "❌ 获取消息失败"
    exit 1
fi
